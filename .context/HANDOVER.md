# Handover

Moved. The live handoff document is [`HANDOFF.md`](../HANDOFF.md) at the
repository root.

This file described the project as of Week 0, saying "no data ingested, no model
trained" and naming Phase 2 as next. It stayed that way through six phases
because nothing consulted it and nothing forced a reread, which is the same
failure mode `HANDOFF.md` describes and which has now produced five defects in
this project.

Keeping two documents with one job is what made the first one go stale, so this
is a pointer rather than a second copy.
